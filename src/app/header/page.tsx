"use client";

import "../../../i18n";
import { HeaderStaking } from "@/components/HeaderStaking";
import { useTranslation } from "react-i18next";

export default function ContactUsPage() {
  const { t } = useTranslation();

  return <HeaderStaking />;
}
